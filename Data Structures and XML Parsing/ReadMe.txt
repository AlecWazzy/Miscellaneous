To run the application, open the command window where parser.jar is located.

Then enter someting like java  -jar parser.jar xmlSample

Where xmlSample is the name of the xml file you wish to parse WITHOUT EXTENSIONS.

The .xml extension is added within the application.