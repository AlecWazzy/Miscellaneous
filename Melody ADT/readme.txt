To run the applications, make sure the command line is pointed at the jar file's directory.

JAR cmd command:
java -jar AWassill2_Melody.jar

Enter notes in the format of "A4" or "G3#"

JAR cmd command:
java -jar AWassill2_Song.jar

For entering songs, do not enter any paths, just the song name i.e. "testsong"